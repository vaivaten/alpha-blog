<h1>Categories</h1>

<div class='container'>
  <div class='row'>
  <% @categories.each do |category| %>
    <div class='col-md-2'>
      <%= link_to "#{category.name}" %>
    </div>
  <% end %>
  </div>
</div>
